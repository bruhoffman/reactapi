export const goToLogin = (navigate) => {
  navigate("/");
};

export const goToSignup = (navigate) => {
  navigate("/signup");
};

export const goToFeed = (navigate) => {
  navigate("/feed");
};

export const goToNewRecipe = (navigate) => {
  navigate("/newRecipe");
};

export const goToFavorites = (navigate) => {
  navigate("/favorites");
};

export const goToRecipe = (navigate) => {
  navigate("/recipe/:id");
};
